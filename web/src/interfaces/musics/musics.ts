export interface Music {
    id: string 
    name: string
    band: string
    duration: string
    composer: string
    categoryId: string
    year: number
    url: string
    createdAt: Date | null
}